# Swaylock config for rectangular input indicator with stars and rounded corners

# Indicator appearance
indicator-radius 20                 # Rounded corners (set to a lower value for more rectangular look)
indicator-thickness 10              # Thickness of the indicator ring
indicator-caps-lock                 # Show caps lock status in the indicator
indicator                           # Enable the input indicator

# Key press feedback colors
key-hl-color 00ff00                 # Highlight color when a key is pressed
bs-hl-color ff0000                  # Highlight color for backspace

# Ring colors (surrounding the indicator)
ring-color 444444                   # Default ring color
ring-ver-color 00ff00               # Verification (successful) ring color
ring-wrong-color ff0000             # Wrong input ring color
ring-clear-color 00ff00             # Ring color for cleared input

# Inside colors (inside the ring)
inside-color 00000066               # Main inside background (semi-transparent black)
inside-ver-color 00000066           # Inside color when verified
inside-wrong-color 00000066         # Inside color when wrong input
inside-clear-color 00000066         # Inside color when input is cleared

# Line colors (text and separation lines)
line-color 000000                   # Default line color
line-clear-color 000000             # Line color when input is cleared
line-wrong-color ff0000             # Line color when wrong input
separator-color 000000              # Separator color

# Text colors and style
text-color ffffff                   # Text color (white stars)
text-clear-color 00ff00             # Text color when input is cleared (green)
text-wrong-color ff0000             # Text color when input is wrong (red)
font monospace                      # Monospace font for stars
font-size 20                        # Font size for stars

# Show failed attempts
show-failed-attempts                # Shows the number of failed attempts

# Background color (can change to your preference)
color 000000                        # Set the background color to black

# Additional options
clock                               # Show a clock while locked
timestr %H:%M                       # Format for the time display (24-hour format)

# Optionally, if you want to blur the background (if swaylock-effects is installed)
#blur 5

